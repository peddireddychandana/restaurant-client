kind = "web"
previewPath = "/"
title = "CRC Multicuisine Restaurant"
version = "1.0.0"
id = "artifacts/crc-restaurant"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25571

[services.development]
run = "pnpm --filter @workspace/crc-restaurant run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/crc-restaurant", "run", "build" ]
publicDir = "artifacts/crc-restaurant/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25571"
BASE_PATH = "/"
